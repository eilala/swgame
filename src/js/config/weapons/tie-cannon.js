/**
 * Configuration for Tie Cannon weapon
 */
export const TieCannonConfig = {
    PRIMARY: {
        DAMAGE: 10,
        ENERGY_COST_PER_BOLT: 2,
        FIRE_RATE: 4, // Shots per second (slower than blaster)
        CONVERGENCE_RANGE: 50,
        SPEED: 60,
        LIFETIME: 10.0,
        SPREAD_DISTANCE: 0.5, // Distance between the two bolts
    }
};